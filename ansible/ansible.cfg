[defaults]
# No default inventory: callers must pass -i explicitly. We keep separate
# per-env inventories under ansible/inventories/ (staging.ini, prod.ini).
remote_user = ec2-user
private_key_file = ./meowgang-store-staging-key.pem
host_key_checking = False